using System;
using System.Collections.Generic;

namespace fifthsharp
{
    class Program
    {
        static void Main(string[] args)
        {
            long N = Convert.ToInt64(Console.ReadLine());
            long K = Convert.ToInt64 (Console.ReadLine());
    
            // List<ValueTuple<long, int>> seq = new List<ValueTuple<long, int>>();
            // List<long> kp;
            // List<long> km;
            // bool havepositive = false;

            // seq.Add((7,4));
            // foreach ((long, int)el in seq)
            // {
            //     Console.WriteLine(el);
                
            // }
            // Console.ReadKey();
        }
    }
}
