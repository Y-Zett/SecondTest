using System;

namespace App
{
    class Program
    {
        static void Main(string[] args)
        {
            long N = Convert.ToInt64(Console.ReadKey());
            long K = Convert.ToInt64(Console.Read());
            Console.WriteLine(N + " " + K);
        }
    }
}
